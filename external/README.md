# Mini-Pytorch3D Externals

This folder should be created by <mini-pytorch3d-root>/scripts/download_resources.sh, which should look like:

```
external/
  ├── stb                # Image IO
  └── libtorch/          # PyTorch C++ API
```